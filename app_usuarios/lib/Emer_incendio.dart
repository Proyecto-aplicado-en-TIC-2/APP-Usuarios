import 'package:flutter/material.dart';

class EmerIncendio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Emergencia de incendio'),
      ),
      body: Center(
        child: Text('Hola mundo'),
      ),
    );
  }
}