import 'package:flutter/material.dart';

class EmerMedica extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Emergencia médica'),
      ),
      body: Center(
        child: Text('Hola mundo'),
      ),
    );
  }
}