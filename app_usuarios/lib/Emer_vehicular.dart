import 'package:flutter/material.dart';

class EmerVehicular extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Accidente vehicular'),
      ),
      body: Center(
        child: Text('Hola mundo'),
      ),
    );
  }
}